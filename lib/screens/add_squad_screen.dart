import 'package:flutter/material.dart';

class AddSquadScreen extends StatelessWidget {
  static const routeName = '/add-squad';
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
